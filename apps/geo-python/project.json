{
  "name": "geo-python",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
        "cwd": "apps/geo-python"
      }
    }
  }
}
